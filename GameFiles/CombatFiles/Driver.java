package CombatFiles;

// import java.util.Random;
import java.util.Scanner;

public class Driver {
    static Scanner sc = new Scanner(System.in);

    public static void main(String[] args) {
        // // Random r = new Random();
        // Armour class2 = new Armour(4, 10);

        // Player player = new Player(100, 6, 4, 6, 6, class2, "Mario");
        // Gun gun = new Gun(new PlayerAbility("Spray & Pray", false, 1, 5, 10, 15),
        // new PlayerAbility("Burst Fire", true, 4, 3, 3, 30),
        // new PlayerAbility("Double Time", true, 9, 4, 2, 40),
        // new PlayerAbility("Single Shot", true, 4, 2, 1, 25));
        // player.gun = gun;

        // CombatEncounter cb = new CombatEncounter(player, 2, 1);
        // CombatUI ui = new CombatUI(cb);
        // cb.ui = ui;

    }
}
